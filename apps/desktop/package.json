{
  "name": "@notes/desktop",
  "version": "0.7.1",
  "description": "Notes — local-first knowledge tool (Electron desktop wrapper)",
  "author": "Notes Contributors",
  "desktopName": "Notes",
  "private": true,
  "main": "dist-electron/main.js",
  "scripts": {
    "build:electron": "tsc -p tsconfig.json",
    "build:server-bundle": "tsdown --config tsdown.server.config.ts && node scripts/copy-server-themes.mjs",
    "build:electron-main": "tsdown --config tsdown.electron.config.ts",
    "dev": "npm run build:electron && cross-env ELECTRON_DEV=1 electron .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "generate-icons": "node scripts/generate-icons.mjs",
    "package": "electron-builder",
    "package:win": "electron-builder --win",
    "package:mac": "electron-builder --mac",
    "package:linux": "electron-builder --linux"
  },
  "devDependencies": {
    "@electron/rebuild": "^4.2.0",
    "cross-env": "^10.1.0",
    "electron": "43.4.1",
    "electron-builder": "^26.15.3",
    "png2icons": "^2.0.1",
    "sharp": "^0.35.3",
    "tsdown": "^0.22.14",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "better-sqlite3": "^13.0.3"
  }
}
